---
description: ROADMAP のタスクを完了にする
argument-hint: <完了にするタスクのキーワード（例: CI導入, OGP）>
allowed-tools: Read, Write
---

以下の手順で docs/ROADMAP.md を更新してください。

1. `docs/ROADMAP.md` を読み込む
2. `$ARGUMENTS` に一致するタスク行を探す
3. `- [ ]` を `- [x]` に変更する
4. 該当行が見つからない場合は、最も適切な Phase セクションの末尾に `- [x] $ARGUMENTS` を追加する
5. 変更内容を報告する
